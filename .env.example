# UniFi OS Server Configuration
# Copy this file to .env and modify as needed

# ============================================================================
# DIRECTORY CONFIGURATION
# ============================================================================

# Directory for persistent data (config, logs, certificates, MongoDB data)
# Use USB SSD path, not SD card!
DATA_DIR=/opt/unifi-os

# ============================================================================
# CORE SETTINGS
# ============================================================================

# Hostname or IP address for this UniFi OS Server
# Devices will use this for adoption: set-inform http://<UOS_SYSTEM_IP>:8080/inform
# Use your static IP or a DNS name (e.g., unifi.home.local)
UOS_SYSTEM_IP=unifi.yourdomain.com

# Timezone ( Olson timezone format )
# Examples: America/New_York, Europe
TZ=UTC

# ============================================================================
# OPTIONAL SETTINGS
# =========================================================================/London, UTC===

# UniFi OS UUID (leave empty to generate automatically)
# Used for device adoption identification
# UOS_UUID=

# Backup directory (defaults to DATA_DIR/backups)
# BACKUP_DIR=/opt/unifi-os/backups

# ============================================================================
# PERFORMANCE TUNING (Advanced)
# ============================================================================

# JVM Maximum Heap Size (reduce if experiencing memory issues)
# JVM_MAX_HEAP_SIZE=1024m

# Enable optimizations for low-power devices or many devices
# LOTSOFDEVICES=true
